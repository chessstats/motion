library(shiny)
library(DT)
library(plotly)


shinyUI(fluidPage(
  
  titlePanel(paste("Historical Fide rating charts of top players")),
  
  dataTableOutput("dataTable"),
  plotlyOutput("trendPlot")
  
))
